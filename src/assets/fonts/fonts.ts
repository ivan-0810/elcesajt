import { Inter, Lora } from "next/font/google";

const inter = Inter({ subsets: ["latin"] }); // eslint-disable-line

const lora = Lora({ subsets: ["latin"] }); // eslint-disable-line

export { inter, lora}